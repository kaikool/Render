{
  "name": "workspace",
  "version": "1.0.0",
  "description": "A Node.js service using Express and Puppeteer to capture authenticated TradingView chart screenshots, optimized for deployment on Render Free Plan.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chrome-aws-lambda": "^10.1.0",
    "express": "^5.1.0",
    "puppeteer": "^24.9.0"
  }
}
